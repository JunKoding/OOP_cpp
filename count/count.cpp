#include <iostream>
#include <string>
#include <vector>


using namespace std;


int main() {
  string s; // 문자열
  int count = 0; // 단어 개수 초기화
  vector<int> counter; // 개수 저장할 vector

  while(getline(cin, s)) {
    cout << s << endl;
  } // 문자열 입력


} 